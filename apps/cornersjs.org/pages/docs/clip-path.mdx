import SemiChamferedButtonCode from "../../wrappedComponents/SemiChamferedButton"

import SemiChamferedButton from "../../components/SemiChamferedButton"
import ReactMarkdown from "react-markdown"

# Clip paths

The `chamfered` preset is a rectangle with all of the corners cleaved off, like this:

By the way, did you notice that in the `chamfered` example, we did this with a `<div>`? Well, we can use corners to generate any sort of DOM element. Let's try a `<button>` next using `semi-chamfered` !

<SemiChamferedButton />

<SemiChamferedButtonCode />
