{
  "name": "@corners/monorepo",
  "scripts": {
    "dev": "echo \"No dev script configured\" && exit 1",
    "build:example": "echo \"No build:example script configured\" && exit 1",
    "predeploy": "pnpm run build:example",
    "deploy": "echo \"No deploy script configured\" && exit 1",
    "build": "echo \"No build script configured\" && exit 1",
    "test": "echo \"No test script configured\" && exit 1",
    "test:once": "echo \"No test:once script configured\" && exit 1",
    "lint": "echo \"No lint script configured\" && exit 1",
    "lint:fix": "echo \"No lint:fix script configured\" && exit 1",
    "release": "pnpm run build && changeset publish",
    "notes": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "2.26.1",
    "nx": "16.4.0"
  },
  "homepage": "https://github.com/jeremybanka/corners#readme",
  "bugs": {
    "url": "https://github.com/jeremybanka/corners/issues"
  }
}
